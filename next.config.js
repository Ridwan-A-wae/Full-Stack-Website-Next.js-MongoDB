/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "unsplash.com"
        ]
    }
}

module.exports = nextConfig
